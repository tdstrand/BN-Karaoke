
{
    
  // Connection strings for database access
  "ConnectionStrings": {
    // PostgreSQL connection string for the Karaoke database
    // Update 'Host', 'Username', and 'Password' based on your environment
    "DefaultConnection": "Server=localhost;Database=BNKaraokeDb;Username=postgres;Password=wordpass"
  },

  
  
    // JWT settings for authentication
    "Jwt": {
      // Secret key for signing JWT tokens (must be at least 32 characters)
      "Key": "!!!BNkaraokekeysbutlowrisk135792468",
      // Issuer of the JWT token (e.g., your app's name)
      "Issuer": "KaraokeApp",
      // Audience for the JWT token (e.g., intended users or clients)
      "Audience": "KaraokeUsers"
    },

    // Karaoke-specific application settings

    "KaraokeSettings": {
      // Maximum number of songs a user can queue
      "MaxSongsPerUser": 8,
      // Duration of a karaoke session in minutes
      "SessionDurationMinutes": 360
    },

    // External service settings (e.g., YouTube API)
    // Note: For security, consider storing sensitive keys in environment variables or user secrets
    "ExternalServices": {
      // API key for YouTube Data API (replace with your actual key)
      "YouTubeApiKey": "your_youtube_api_key_here"
    },

    // Logging configuration
    "Logging": {
      "LogLevel": {
        // Default log level for the application
        "Default": "Information",
        // Log level for ASP.NET Core framework logs
        "Microsoft.AspNetCore": "Warning"
      }
    },

    // Allowed hosts for the application (use "*" to allow all hosts)
    "AllowedHosts": "*"
  }